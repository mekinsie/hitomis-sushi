  <%= render "layouts/errors", :object => @review %>

<%= form_for [@product, @review] do |f| %>
  <%= f.label :author %><br>
  <%= f.text_field :author%><br>
  <%= f.label :rating %><br>
  <%= f.select :rating, [1,2,3,4,5] %><br>
  <%= f.label :content_body %><br>
  <%= f.text_area :content_body, size: "40x10" %><br>
  <%= f.submit :Submit %>
<% end %>

<%= link_to "Back", product_path(@product) %>